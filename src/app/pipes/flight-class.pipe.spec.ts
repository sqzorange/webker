import { FlightClassPipe } from './flight-class.pipe';

describe('FlightClassPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightClassPipe();
    expect(pipe).toBeTruthy();
  });
});
